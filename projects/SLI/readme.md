# Домашнее задание
#### Тема:
 Конкурентный поиск дубликатов файлов (подготовка к проекту).
#### Зачем:
 В лабораторной мы считали размеры. В домашке мы будем искать файлы с одинаковым размером и именем (простая проверка на дубликаты), что приближает нас к реальным задачам обработки данных.
### Задание:
1. Написать CLI-утилиту, которая принимает путь к директории через флаг -dir (используйте пакет flag).
2. Утилита должна рекурсивно обойти директорию.
3. Логика:
   - Собрать информацию о всех файлах: имя и размер.
   - Найти файлы, у которых совпадают и имя, и размер (подозрение на дубликаты).
   - Вывести список таких "подозрительных" групп.
4. Требования:
   - Использовать Worker Pool или Семафор (флаг -workers по умолчанию 5).
   - Обязательно выводить прогресс в консоль (например, "Обработано файлов: 100..."). Для этого можно использовать atomic.AddInt64 в отдельном счетчике или ticker.
### Критерии приемки:
   - Код отформатирован (go fmt).
   - Используется sync.WaitGroup.
   - Присутствует README с примером запуска.
   - Дополнительно: Реализовать интерфейс DuplicateFinder, который может иметь разные стратегии поиска (по имени+размеру или по хешу).
  
# command
``go run . -path C:\Users\kosti\Documents\go\projects\projectNoName -mode name_size   ``
#### result:
```
🚀 Запуск DupliFinder
📂 Папка: C:\Users\kosti\Documents\go\projects\projectNoName
⚙ Режим: name_size
👷‍♂️👷‍♀️ Воркеров: 8



📊 Результаты поиска:
Дубликаты не найдены

⏱  Время выполнения: 522.4µs
PS C:\Users\kosti\Documents\go\projects\sli> go fmt
main.go
```